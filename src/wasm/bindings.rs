// WASM bindings stub
// Full implementation requires careful type erasure due to type-state pattern
// This is a placeholder for future WASM integration

pub struct WasmMigrationPipeline;

impl WasmMigrationPipeline {
    pub fn new() -> Self {
        Self
    }
}

impl Default for WasmMigrationPipeline {
    fn default() -> Self {
        Self::new()
    }
}
